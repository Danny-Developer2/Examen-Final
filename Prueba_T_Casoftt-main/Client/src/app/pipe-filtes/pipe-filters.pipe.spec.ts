import { PipeFiltersPipe } from "./pipe-filters.pipe";

describe('PipeFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
